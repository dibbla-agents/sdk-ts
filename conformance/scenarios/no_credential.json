{
  "name": "no_credential",
  "description": "With no API token and no identity token file the worker still connects, sending no authorization metadata (the server decides).",
  "worker": { "unset": ["SERVER_API_TOKEN"] },
  "steps": [
    { "accept": { "metadata": { "authorization": null, "x-org-id": null } } },
    { "include": "handshake" }
  ]
}
